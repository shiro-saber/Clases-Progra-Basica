/** 
 * Recibir del usuario un número e
 * imprimir si dicho número es par o impar
 */

// Pide el número al usuario.
var numero = prompt("Dame un número");

// inline if
numero % 2 == 0 ? console.log("Es número par") : console.log("Es impar");
